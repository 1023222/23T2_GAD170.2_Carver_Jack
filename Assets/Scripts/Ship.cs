using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ship : MonoBehaviour
{
    //Use lists because Array sizes are FIXED and the sizes are going to be changing.

    private void Start()
    {
        List<Crew> applicants = new List<Crew>();
    }

}
